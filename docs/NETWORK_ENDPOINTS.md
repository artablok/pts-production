# Network endpoints

## Public P2P seed node

```
HOST: 188.137.177.230
PORT: 39012
```

Verified externally reachable (confirmed via `Test-NetConnection` /
`nc -zv` from outside the host network).

## Explorer

https://explorer.ptscrypto.com

## RPC access

RPC ports are intentionally not publicly exposed — this network is not
yet designed for public transaction submission. To read chain state
independently, sync your own node (see
[`RUN_A_NODE.md`](./RUN_A_NODE.md)) and query your own copy.
